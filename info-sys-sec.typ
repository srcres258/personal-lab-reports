#import "template/lib.typ": documentclass

#let (
  doc, cover, outline-page, experiment,
  content-area, reset-page-number,
) = documentclass(
  is-practice: false,
  show-day: true,
  university-logo: "../assets/tyut-logo.png",
  info: (
    course-name: "信息系统安全",
    experiment-name: "信息系统安全实验",
    location: "实验室E210",
    class-name: "区块链2301",
    student-id: "2023002089",
    student-name: "胡皓文",
    supervisor: "胡钊鸣",
    year: 2026,
    month: 5,
    day: 7,
  ),
)

#show: doc

#cover()

#outline-page()

#reset-page-number
#show: content-area

// ========== 实验一 数据恢复与安全删除 ==========

#experiment(title: "实验一 数据恢复与安全删除")[
  == 一. 实验目的

  掌握信息系统安全实验环境的构建及存储介质的数据恢复和安全删除技术。

  == 二. 实验内容

  + 用 Easy Recovery 工具恢复已删除的文件；
  + 用 WinHex 工具恢复已删除的文件；
  + 用 Eraser 工具安全删除文件。

  == 三. 实验正文

  本次实验以 WinHex 工具恢复小文件为例，展示数据恢复的完整流程。

  + 在 E 盘创建文件"实验.txt"，写入内容"计算机病毒防护技术"。

  + 定位 MFT 起始逻辑扇区。在 WinHex 界面上半部分文件列表中，找到 MFT 对应的 1st sector 值，跳转至该扇区，该扇区即为第一个文件记录的起始位置。NTFS 文件系统中，每个文件和目录在 MFT 中均有一条对应的记录，MFT 起始扇区是定位所有文件记录的入口。

  + 定位目标文件记录。查找文件列表中"实验.txt"对应的 1st sector 值，跳转至对应扇区。该扇区起始字符为"FILE"，且界面左下部分"Alloc. of visible drive space"显示为"实验.txt"，确认已定位到目标文件的 MFT 记录。

  + 分析文件记录属性。NTFS 文件系统中，MFT 记录包含多个属性，各属性由类型标识区分。以文件记录起始字节为基准，依次分析：
    - 偏移量 0x14 连续 2 字节值为 0x0038，说明第一个属性的偏移量为 0x38；
    - 偏移量 0x16 连续 2 字节值为 0x0010，说明文件处于使用状态，未被删除；
    - 偏移量 0x38 起连续 4 字节为 0x10000000，标识该属性为 10H 类型属性（标准信息属性）。偏移量 0x10 起连续 4 字节为 0x48，说明该属性除头部外长度为 72 字节；偏移量 0x14 起连续 2 字节为 0x18，说明属性内容起始偏移为 0x18，后续 72 字节为 10H 属性的具体内容；
    - 10H 属性结束后，连续 4 字节为 0x30000000，标识为 30H 类型属性（文件名信息属性）。以 30H 起始地址为基准，偏移量 0x10 处值为 4EH（即 78 字节），表示 30H 属性总长度为 78 字节；偏移量 0x14 连续 2 字节为 0x18，说明属性内容起始偏移为 0x18；偏移量 0x00 处值为 0x06，说明文件名为 6 个字符；偏移量 0x42 起 14 字节为 Unicode 编码的文件名"实验.txt"；
    - 30H 之后为 40H 类型属性，分析方式与前序属性相同，此处不再展开；
    - 40H 属性结束后，连续 4 字节为 0x80000000，标识为 80H 类型属性（文件数据属性）。以 80H 类型属性起始地址为基准，偏移量 0x08 处 1 字节为 0x00，说明该属性为常驻属性，数据直接存储在 MFT 记录中，无需占用额外磁盘空间。偏移量 0x10 连续 2 字节为 0x12，说明属性除头部外长度为 18 字节；偏移量 0x14 连续 2 字节为 0x18，说明属性内容起始偏移为 0x18，后续 18 字节即为文件"实验.txt"的完整数据内容（"计算机病毒防护技术"共 9 个汉字，每个汉字占 2 字节，合计 18 字节，与分析结果一致）。

  + 删除文件后的扇区变化。删除"实验.txt"后，重新加载磁盘，文件列表仍显示该文件。跳转至原 MFT 扇区，观察到文件记录偏移量 0x16 处连续 2 字节由 0x0010 变为 0x0000，标识文件已被删除。此时文件的实际数据内容仍保留在 MFT 记录中，未被覆盖，具备恢复条件。

  + 恢复文件。找到已删除文件的 80H 类型属性，选中对应数据块（偏移量 0x18 起 18 字节），点击 WinHex 工具栏"Edit" → "Copy Block" → "Into New File"，保存为新文件"实验.txt"，即完成文件恢复。经检查，恢复后的文件内容与原始内容一致。
]

// ========== 实验二 口令攻击与防护 ==========

#experiment(title: "实验二 口令攻击与防护")[
  == 一. 实验目的

  掌握各信息系统的口令储存方法，以及口令攻击和防护技术。

  == 二. 实验内容

  + 本地 Windows 口令破解：使用 LC6 软件通过字典攻击和暴力破解方式破解本地 Windows 系统口令；
  + 彩虹表口令破解：使用 Ophcrack 工具配合开源彩虹表破解 Windows 环境口令；
  + FTP 服务口令枚举：架设 FileZilla FTP 服务器，使用 Fscan 远程口令枚举工具进行字典攻击。

  == 三. 实验正文

  - *3.1 本地 Windows 口令破解*

  安装 LC6 软件和 PWDUMP 软件。PWDUMP 用于从 SAM 文件中提取 Windows 系统口令哈希值，随后在 LC6 中导入哈希值进行口令破解。

  首先采用字典攻击方式。在 LC6 中配置字典文件路径，字典文件包含常见口令候选词。LC6 逐一计算字典中每个词的哈希值，与目标哈希值进行比对，若匹配成功则输出对应的明文口令。字典攻击速度快，但受限于字典的覆盖范围。

  随后采用暴力破解方式。与字典攻击不同，暴力破解不依赖预设词表，而是穷举字符空间中的所有可能组合。LC6 从最小长度开始，逐步遍历所有字符组合（包括大小写字母、数字、特殊符号），直至找到匹配项或穷举完设定范围。实验观察到，暴力破解的耗时随口令长度和字符集的增大呈指数级增长。

  - *3.2 彩虹表口令破解*

  安装 Ophcrack 工具，下载开源彩虹表文件（rtc 格式）。彩虹表是一种预先计算好的哈希值与明文对照表，通过时间-空间权衡策略，在有限存储空间下实现快速的哈希逆向查询。

  在 Ophcrack 中加载彩虹表文件，导入从目标系统导出的口令哈希值。Ophcrack 利用彩虹链技术，在哈希链上查找匹配项，若哈希值命中彩虹表覆盖的区间，即可在短时间内还原出明文口令。实验观察到，彩虹表破解速度显著优于传统暴力破解，但其光盘体积较大，且仅对彩虹表覆盖的字符集和口令长度范围有效。

  - *3.3 FTP 服务口令枚举*

  在 Windows 环境中安装并架设 FileZilla FTP 服务器，创建测试用户账户。确认 FTP 服务正常运行后，在攻击端使用 Fscan 远程口令枚举工具执行字典攻击。

  配置 Fscan 的目标 IP 为 FTP 服务器地址，端口为默认的 21 端口，加载口令字典文件。Fscan 自动尝试字典中的每个用户名与口令组合，向 FTP 服务器发起登录请求。

  为验证攻击行为，预先在 FileZilla Server 中启用日志记录功能。攻击结束后，进入 FileZilla Server 安装目录下的 Logs 子目录，打开 `FileZilla Server.log` 文件，可查看到来自攻击端 IP 的大量连接尝试记录，每次失败的登录尝试均被详细记录，包括时间戳、客户端 IP、尝试的用户名及认证结果。
]

// ========== 实验三 Windows 系统安全机制及服务器配置 ==========

#experiment(title: "实验三 Windows系统安全机制及服务器配置")[
  == 一. 实验目的

  掌握 Windows 操作系统本地安全设置、账户和口令安全设置、审核策略及其他安全策略；掌握注册表配置、文件/文件夹权限设置及日志分析方法。

  == 二. 实验内容

  + 本地安全策略配置：使用 `secpol.msc` 配置密码策略、账户锁定策略、审核策略、IP 安全策略等；
  + 注册表配置：使用 `regedit` 修改网卡 MAC 地址；
  + 文件及文件夹权限设置：创建用户并配置 NTFS 访问控制权限；
  + 审核日志分析：查看系统日志，分析事件 ID 及日志类型含义。

  == 三. 实验正文

  - *3.1 本地安全策略配置*

  使用 Win+R 输入 `secpol.msc` 打开本地安全策略控制台，或通过 `mmc` 添加安全策略管理单元。依次完成以下配置项。

  账户策略——密码策略：设置密码长度最小值为 6 个字符，配置密码最长存留期和最短存留期，启用强制密码历史以防止重复使用近期密码。配置完成后，切换普通用户尝试修改密码，当输入密码长度小于 6 字符时，系统提示"密码不满足密码策略的要求"，修改被拒绝。

  账户策略——账户锁定策略：设置账户锁定阈值为 5 次无效登录尝试，锁定持续时间 30 分钟，锁定计数复位时间 30 分钟。阈值设置为 5 次可在阻止暴力破解的同时，避免因误操作导致的频繁锁定。

  账户和口令安全设置：检查本地用户列表，删除不必要的账户（如 User 用户、测试用户、共享用户等）；禁用 Guest 账户以消除匿名访问隐患；创建 2 个管理员账号用于日常管理和应急恢复；创建陷阱用户，用户名为 Administrator，权限设为最低，用于迷惑和延迟攻击者；配置系统不显示上次登录的用户名，避免攻击者获取有效账户信息。

  审核策略：启用审核策略更改、审核账户登录事件、审核账户管理、审核登录事件、审核特权使用。审核策略更改记录策略修改操作；审核账户登录事件记录每次登录尝试；审核登录事件记录账户登录与注销活动；审核特权使用记录敏感操作（如更改系统时间、安装驱动等）。

  IP 安全策略：自定义 IP 过滤规则，配置安全规则以限制进出站流量。通过 IP 安全策略可实现基于 IP 地址、协议类型、端口的访问控制，构成主机级防火墙的补充。

  其他设置：配置公钥策略，管理加密文件系统（EFS）证书；配置软件限制策略，通过路径规则或哈希规则限制未授权软件的执行。

  - *3.2 注册表配置*

  使用 Win+R 输入 `regedit` 打开注册表编辑器。修改网卡 MAC 地址：定位至 `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Class\{4D36E972-E325-11CE-BFC1-08002BE10318}` 下的对应网卡子键，找到或新建 `NetworkAddress` 字符串值，将其设置为目标 MAC 地址（12 位十六进制字符）。修改后需重启网卡或重新启用网络连接使配置生效。注册表直接修改 MAC 地址可用于网络安全测试中绕过基于 MAC 的访问控制，但应注意合法合规使用。

  - *3.3 文件及文件夹权限设置*

  首先创建两个本地用户账户：student 和 teacher。使用 teacher 账户登录系统，在 C 盘创建文件夹 `C:\tmp`，在该文件夹内新建文件 `hello.txt`，文件内容为"hello student."。

  右键 `C:\tmp` 文件夹，进入"属性"→"安全"选项卡，编辑访问控制权限。在权限设置中添加 student 用户，为其分配以下权限：允许"读取和执行"、"列出文件夹内容"、"读取"；拒绝"写入"和"修改"。权限配置完成后，切换至 student 账户登录，尝试打开 `hello.txt` 文件可以正常读取内容；尝试修改文件内容或删除文件时，系统提示权限不足，操作被拒绝。该实验验证了 NTFS 文件系统细粒度访问控制的有效性。

  - *3.4 审核日志分析*

  通过"计算机管理"→"事件查看器"查找并查看三类日志：应用程序日志、安全性日志、系统日志。

  应用程序日志记录应用程序运行时产生的事件，包括程序错误、警告和信息性消息。安全性日志记录与安全相关的审计事件，依赖于已启用的审核策略。系统日志记录 Windows 系统组件产生的事件，如设备驱动加载失败、系统服务启动和停止等。

  在系统日志中查看关键事件：事件 ID 12 对应系统启动事件，记录操作系统启动的时间点；事件 ID 13 对应系统关闭事件，记录系统正常关闭的时间点。通过分析相邻的 ID 12 和 ID 13 之间的时间差，可确定系统运行时长。

  日志按严重程度分为三种类型：警告类型表示可能影响系统或应用程序运行但尚未导致立即故障的情况，如磁盘空间不足、网络连接延迟等；信息类型记录正常的系统操作信息，如服务启动成功、驱动加载完成等；错误类型表示已发生的故障，如服务启动失败、设备驱动异常等。通过分析各类日志的时间分布和频率，可辅助判断系统安全状态和异常行为。
]

// ========== 实验四 SqlServer 安全机制及应用 ==========

#experiment(title: "实验四 SqlServer安全机制及应用")[
  == 一. 实验目的

  掌握 SqlServer 核心安全机制，理解身份认证与访问控制原理；掌握常见 SQL 注入攻击方式及实现。

  == 二. 实验内容

  + SqlServer 安全配置：安装 SQL Server 2005，配置身份验证模式和三级授权管理；
  + SQL 注入攻击实践：在 SQLi-labs 和 DVWA 平台上实践重言式注入、Union 注入、基于时间的盲注、存储型 XSS 攻击。

  == 三. 实验正文

  - *3.1 SqlServer 安全配置*

  安装 SQL Server 2005 软件，在安装过程中选择身份验证模式。SQL Server 支持两种模式：Windows 身份验证模式仅允许通过 Windows 账户登录，安全性较高但灵活性有限；混合身份验证模式同时支持 Windows 账户和 SQL Server 专用账户，兼容更多应用场景。

  配置完成后实现三级授权管理。服务器级权限控制对 SQL Server 实例的整体操作（如创建数据库、管理登录账户）；数据库级权限控制对特定数据库的访问和操作（如读写表、执行存储过程）；对象级权限精确控制对特定表、视图、存储过程等对象的操作权限。通过三级授权可实现最小权限原则。

  - *3.2 SQL 注入攻击实践*

  实验环境：两台 Windows 7 主机，一台作为攻击机（IP: 192.168.1.140），一台作为 Web 服务器（IP: 192.168.1.66）。Web 服务器端安装 XAMPP，部署 SQLi-labs 和 DVWA 应用。

  *3.2.1 重言式 SQL 注入*

  访问 `http://192.168.1.66/sqli/Less-11/` 进入登录页面。合法用户输入正确的用户名和口令，可正常登录系统。

  攻击者在 Username 字段输入 `test' or 1=1#`，Password 字段输入任意值，即可绕过认证直接登录，系统以数据库中首个用户身份进入系统。原理分析：注入语句被拼接到后端 SQL 查询中，形成类似 `SELECT * FROM users WHERE username='test' or 1=1#' AND password='...'` 的语句。`1=1` 为恒真条件，使 WHERE 子句对所有记录均成立，从而跳过口令校验；`#` 符号注释掉后续的原始口令比对部分。

  *3.2.2 Union 注入*

  重言式注入仅能绕过登录认证，无法获取数据库中的实际数据。Union 注入利用 SQL 的 UNION 操作符，将攻击者构造的查询结果与原始查询结果合并返回，从而窃取数据库信息。攻击流程为：确定数据库名 → 确定表名 → 确定表结构 → 提取数据。

  以 MySQL 为例，通过 Union 联合查询拼接 `information_schema` 系统数据库查询语句。首先使用 `UNION SELECT 1,2,database()` 确定当前数据库名称；再查询 `information_schema.tables` 获取目标数据库中的表名列表；然后通过 `information_schema.columns` 查询各表的字段名和数据类型；最后构造 Union 查询提取目标表中的用户名、口令等敏感数据。

  *3.2.3 基于时间的盲注*

  当目标应用关闭了错误回显且 Union 返回结果不可见时，可使用基于时间的盲注获取信息。注入语句示例：`' AND IF(ASCII(SUBSTR(DATABASE(),1,1))=114,SLEEP(5),1)#`。该语句判断当前数据库名第一个字符的 ASCII 码是否为 114（即字符'r'），若是则触发 SLEEP(5) 函数使响应延迟 5 秒，否则立即返回。

  攻击者通过观测页面响应时间，逐字符推断数据库名称、表名、字段名及字段值。虽然盲注速度较慢，但在无回显条件下是有效的信息提取手段。

  *3.2.4 存储型 XSS 攻击（DVWA）*

  实验环境扩展为三台 Windows 7 主机，分别作为攻击者、受害者和 Web 服务器。DVWA 安全级别设为 Low，攻击机安装 Firefox 浏览器及 Live HTTP Headers 组件。

  攻击步骤：攻击者在 DVWA 的存储型 XSS 漏洞页面插入恶意 JavaScript 脚本，该脚本窃取访问者的 Cookie（包含 SessionID），并将其发送至攻击者控制的接收端。受害者在不知情的情况下访问被注入恶意脚本的页面，其 SessionID 被自动窃取。攻击者利用获得的 SessionID，在本地浏览器中设置 Cookie，冒充受害者身份访问 DVWA 应用，从而绕过正常认证流程，以受害者权限执行操作。

  存储型 XSS 的持久化特性使其危害尤为严重——一次注入可影响所有访问该页面的用户，且无需用户额外交互。防御存储型 XSS 的关键在于对所有用户输入进行严格的输入验证和输出编码，确保恶意脚本无法被存储或执行。
]
